# Grafana Dashboards Provisioning
apiVersion: 1

providers:
  - name: 'ParkShare Dashboards'
    orgId: 1
    folder: 'ParkShare'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
