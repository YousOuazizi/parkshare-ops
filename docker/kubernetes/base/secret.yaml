# NOTE: This is a template. DO NOT commit actual secrets to version control.
# Use external secrets management (Sealed Secrets, External Secrets Operator, etc.)
apiVersion: v1
kind: Secret
metadata:
  name: parkshare-secrets
  namespace: parkshare
type: Opaque
stringData:
  database-url: "postgresql://user:password@postgres:5432/parkshare"
  redis-password: "changeme"
  jwt-secret: "changeme-jwt-secret"
  jwt-refresh-secret: "changeme-jwt-refresh-secret"
  stripe-secret-key: "sk_test_xxx"
  twilio-account-sid: "ACxxx"
  twilio-auth-token: "xxx"
  aws-access-key-id: "xxx"
  aws-secret-access-key: "xxx"
